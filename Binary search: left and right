# Binary Search 

| | |
| :--- | :--- |
| **Input** | Standard input |
| **Output** | Standard output |

### Problem Statement
Given two arrays, determine for each element of the second array whether it exists in the first array.

### Input Format
- The first line contains two natural numbers $N$ and $K$ ($1 \le N, K \le 100,000$).
- The second line contains $N$ elements of the first array, sorted in ascending order.
- The third line contains $K$ elements of the second array.
- All array elements are integers, each not exceeding $10^9$ in absolute value.

### Output Format
For each of the $K$ numbers, print "**YES**" on a new line if the number exists in the first array, and "**NO**" otherwise.

### Examples
| Input | Output |
| :--- | :--- |
| `5 4` <br> `1 4 5 8 9` <br> `5 6 1 9` | `YES` <br> `NO` <br> `YES` <br> `YES` |

#include <iostream>

using namespace std;

int main() {
    int N, K;
    cin >> N >> K;
    
    int* arr = new int[N];
    for (int i = 0; i < N; i++) {
        cin >> arr[i];
    }
    for (int i = 0; i < K; i++) {
        int a;
        cin >> a;
        
        int found = 0;
        int left = 0, right = N - 1;
        while (left <= right) {
            int mid = left + (right - left) / 2;
            
            if (arr[mid] == a) {
                found = 1;
                break;
            } else if (arr[mid] < a) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        
        if (found) {
            cout << "YES\n";
        } else {
            cout << "NO\n";
        }
    }
    
    delete[] arr;
    return 0;
}


# Animal Counting 

| | |
| :--- | :--- |
| **Input** | Standard input |
| **Output** | Standard output |

### Problem Statement
The Institute of Arts, Mutants, and Information Technology breeds cute colorful animals. For convenience, each color is assigned a unique number (up to $10^9$). One day, all the animals lined up in a row in non-decreasing order of their colors. The lab assistants decided to count how many animals of specific colors live in the nursery and asked you to write a program to help them.

### Input Format
- The first line contains an integer $N$ ($0 \le N \le 10^5$) — the total number of animals.
- The second line contains $N$ non-negative integers sorted in non-decreasing order — the color of each animal (each color $\le 10^9$).
- The third line contains an integer $M$ ($1 \le M \le 100,000$) — the number of queries.
- The fourth line contains $M$ non-negative integers (up to $10^9 + 1$) representing the colors to be counted.

### Output Format
For each query, output a single integer on a new line — the number of animals of the specified color in the nursery.

### Examples
| Input | Output |
| :--- | :--- |
| `10` <br> `1 1 3 3 5 7 9 18 18 57` <br> `5` <br> `57 3 9 1 179` | `1` <br> `2` <br> `1` <br> `2` <br> `0` |

#include <iostream>

using namespace std;

int main() {
    int N;
    cin >> N;
    
    int* arr = new int[N];
    for (int i = 0; i < N; i++) {
        cin >> arr[i];
    } 
    int M;
    cin >> M;
    
    for (int i = 0; i < M; i++) {
        int a;
        cin >> a;
        int left = 0, right = N - 1;
        int first = -1;
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            
            if (arr[mid] == a) {
                first = mid;
                right = mid - 1;
            } else if (arr[mid] < a) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        left = 0;
        right = N - 1;
        int last = -1;
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            
            if (arr[mid] == a) {
                last = mid;
                left = mid + 1;
            } else if (arr[mid] < a) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        
        if (first == -1) {
            cout << "0\n";
        } else {
            cout << last - first + 1 << "\n";
        }
    }
    
    delete[] arr;
    return 0;
}
